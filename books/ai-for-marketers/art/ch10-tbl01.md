<!--
표 10-1 | ch10-tbl01
삽입 위치: 10장 "진단 순서 — 추측하지 말고 순서를 따른다" 절,
  본문에 박힌 호출 지점 [표 10-1 | art/ch10-tbl01.md] 블록 자리(원고 line 101).
  (본문 line 91~99가 진단 순서 4단계를 한 단계씩 문단으로 풀어 쓴다. 글이 각 단계에서
   '무엇을 보고 어떻게 가르는가'를 서술하고, 이 표는 "단계 → 무엇을 보나 → 잡히는
   패턴 → 응급 처치"를 4행으로 나란히 세워 한눈에 대조하게 한다. 비교 항목이 4개(4단계)
   x 4열이라 표가 맞다. 플로차트(그림 10-1)는 '두 방어선의 갈림'이라는 구조를, 이 표는
   '첫 방어선 안의 4단계 순서표'를 담아 역할이 겹치지 않는다.)
캡션: 표 10-1. 진단 순서 네 단계와 각 단계가 잡아내는 패턴.
  끝(출력 자리)에서 시작해 거슬러 올라가며 한 단계씩 정상 여부를 확인해, 문제가 어느
  구간에 있는지부터 가른다. 1단계 출력 자리에서 '통째로 빔→연결 / 부분만 빔→소스'로
  방향을 정하고, 2단계 연결에서 패턴 1·2를, 3단계 소스에서 패턴 5·6을, 4단계 실행
  기록에서 한도(429)와 모델 호출 실패를 잡는다. 이 순서를 밟으면 여섯 패턴의 대부분이
  30분 안에 어느 구간이 범인인지 격리된다. (무음 실패인 패턴 7은 이 순서로 잡히지 않는다
  — 4장 검수의 몫이다. 그림 10-1 참조.)
대체 텍스트: 진단 순서는 네 단계다. 끝(출력)에서 시작해 소스 쪽으로 거슬러 올라가며,
  한 번에 하나씩만 확인해 문제 구간을 좁힌다. 1단계는 출력 자리를 보는 것이다 — 결과가
  도착해야 할 시트·슬라이드를 보고, 통째로 비었는지(그러면 연결을 의심해 2단계로) 특정
  칸만 비었는지(그러면 소스를 의심해 3단계로) 슬라이드만 안 됐는지를 가른다. 이 한 번의
  구분이 나머지 진단의 방향을 정한다. 2단계는 연결 상태를 보는 것이다 — 노코드 도구의
  연결 관리 화면을 열어 만료 표시가 뜬 연결이나 꺼진 워크플로가 있는지 본다. 있으면
  연결이 시간이 지나 만료된 패턴 1이거나 비밀번호·권한 변경으로 끊긴 패턴 2이고, 응급
  처치는 재연결한 뒤 꺼진 워크플로를 다시 켜고 한 번 수동 실행하는 것이다. 보고서가
  통째로 안 온 경우의 대부분이 여기서 끝난다. 단 네이버처럼 키를 발급해 붙여넣는 서명형
  연결이 끊겼으면 재연결 버튼이 없으므로, 5장 플랜 B로 그 채널만 손으로 받아 올려 이번
  주를 살린다. 3단계는 소스를 직접 여는 것이다 — 출력의 특정 칸만 비었을 때, 광고 리포트나
  원본 시트, 또는 모니터링이 긁어오는 경쟁사 페이지를 브라우저로 열어 열 이름이나 화면
  구조가 바뀌었는지 눈으로 확인한다. 소스가 형식을 바꿔 칸이 빈 패턴 5와 소스 사이트가
  화면을 바꿔 브리핑이 빈 패턴 6이 여기서 잡히고, 응급 처치는 이번 주는 그 칸을 손으로
  채우고 매핑 수리는 회의 후로 미루는 것이다. 4단계는 실행 기록을 보는 것이다 — 위 셋으로
  안 잡히면 노코드 도구의 실행 기록 화면을 연다. 로그를 본다는 것은 코드를 읽는 게 아니라,
  어느 단계가 초록불이고 어느 단계에서 빨간불이 켜졌는지를 화면으로 보는 것이다. 빨간불
  옆에 429가 보이면 한도를 넘은 패턴 4이므로 실행 주기를 늦추고, 빨간불이 AI 호출 단계에
  켜져 있고 모델 이름과 함께 오류가 떠 있으면 박아 둔 모델이 은퇴한 패턴 3이므로 살아 있는
  모델로 갈아끼운다. 네 단계를 순서대로 밟으면 여섯 패턴의 대부분은 30분 안에 어느 구간이
  범인인지 격리된다. 패턴 7 무음 실패만은 모든 단계에 초록불이라 이 순서로 잡히지 않으며,
  그것은 4장 검수의 몫이다.
본문/자료카드 정합성: 4단계 순서(출력 자리→연결 상태→소스→실행 기록)·각 단계가 잡는
  패턴·1단계의 방향 가르기(통째 빔→연결 / 부분 빔→소스)는 원고 line 91~99와 일치
  (자료카드 카드 10). 패턴 1·2=연결, 5·6=소스, 4=429, 3=모델 호출 실패 대응은 원고
  line 95~99·101과 일치. 네이버 서명형 예외는 원고 line 113~115·149와 일치(카드 11).
  패턴 7은 이 순서로 안 잡힘은 원고 line 19·129와 일치. 모델 경계(안 나옴=10장/톤만=12장)는
  표의 패턴 3 칸에 "안 나옴" 쪽만 정확히 반영(원고 line 55, 카드 09). 임의 수치 없음 —
  '30분'은 본문의 목표 시간(원고 line 103), '429'는 본문이 쓰는 표준 오류 문구(원고 line 61·99).
무결성(ch06 H2): 가짜 에러 메시지 없음 — 오류 문구는 본문이 쓰는 표준 "429"만. 가짜
  장애 사례·가짜 경쟁사명·가짜 수치 없음. 응급 처치는 사람이 누르는 버튼·여는 화면 수준으로만.
페르소나 가드레일: 코드 노출 0. "로그를 본다"=실행 기록 화면에서 빨간불/초록불 보기.
  패턴 번호 1~7 일관. 강조색은 표라 미사용(조판 시 헤더행만 잉크색 음영, 강조색 셀 없음).
조판 지시: 마크다운 표 그대로 조판. 헤더 1행, 셀 2줄 이내(style.md 7절). 헤더행 배경은
  중립 회색(#F0F4F8), 강조색(#D9480F) 셀 없음(이 표는 순서 대조표라 단일 강조 대상 없음).
  패턴 번호 굵게.
-->

| 단계 | 무엇을 보나 | 잡히는 패턴 | 이번 주 응급 처치 |
|---|---|---|---|
| **1. 출력 자리** | 결과가 도착할 시트·슬라이드. 통째로 비었나, 특정 칸만 비었나, 슬라이드만 안 됐나. | (방향 가르기) 통째 빔→2단계 연결 / 부분만 빔→3단계 소스 | — 방향만 정한다 |
| **2. 연결 상태** | 노코드 도구의 연결 관리 화면. 만료 표시·꺼진 워크플로가 있나. | **패턴 1** 연결 만료 / **패턴 2** 비밀번호·권한 변경 | 재연결 → 다시 켜고 한 번 수동 실행. (네이버 서명형이면 5장 플랜 B로 수동 우회) |
| **3. 소스** | 광고 리포트·원본 시트·경쟁사 페이지를 직접 연다. 열 이름·화면이 바뀌었나. | **패턴 5** 소스 형식 변경 / **패턴 6** 소스 화면 변경 | 이번 주는 그 칸을 손으로 채운다. 매핑 수리는 회의 후. |
| **4. 실행 기록** | 도구의 실행 기록 화면. 어느 단계 빨간불인가, 옆에 무슨 표시인가. | **패턴 4** 한도(빨간불 옆 "429") / **패턴 3** 모델 호출 실패(모델 이름 옆 오류, 출력이 안 나옴) | 429면 실행 주기를 늦춘다. 모델이 죽었으면 살아 있는 모델로 갈아끼운다. |

<!--
별도 주: 패턴 7(무음 실패)은 위 네 단계로 잡히지 않는다 — 모든 단계에 초록불이라
좁힐 단서가 없다. 보고서가 "왔어도" 4장 검수를 돌리는 것이 그 패턴의 유일한 그물이다.
(그림 10-1 참조.) 모델 경계: 출력이 "아예 안 나오면" 패턴 3으로 이 표에서 응급 복구하고,
출력은 나오는데 "톤·골격만 바뀌었으면" 12장 갈아타기 프로토콜의 일이다(원고 line 55).
-->
